<%= form_for(@game_room) do |f| %>
  <% if @game_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_room.errors.count, "error") %> prohibited this game_room from being saved:</h2>

      <ul>
      <% @game_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :maxplayers %><br />
    <%= f.number_field :maxplayers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
